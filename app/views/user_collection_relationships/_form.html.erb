<%= form_with(model: user_collection_relationship) do |form| %>
  <% if user_collection_relationship.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_collection_relationship.errors.count, "error") %> prohibited this user_collection_relationship from being saved:</h2>

      <ul>
        <% user_collection_relationship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
